// AI Service is currently DISABLED/DELETED as per user request.
// This file acts as a placeholder so the server doesn't crash on imports.
// You can implement your new API here later.

/**
 * Placeholder for future AI implementation.
 * Currently returns null to simulate "No Feature".
 */
export const getFaceDescriptor = async (imageInput) => {
    console.log("[AI Service] API is currently disabled/deleted.");
    return null;
};

/**
 * Placeholder for future AI implementation.
 * Currently returns empty array.
 */
export const getAllFaceDescriptors = async (imageInput) => {
    console.log("[AI Service] API is currently disabled/deleted.");
    return [];
};

/**
 * Placeholder for matching logic.
 */
export const isMatch = (descriptor1, descriptor2) => {
    return false;
};
