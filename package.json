{
    "name": "wedding-moments-root",
    "version": "1.0.0",
    "description": "Root package to handle Render deployment for backend",
    "scripts": {
        "build": "cd backend && npm install",
        "start": "cd backend && npm start"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}